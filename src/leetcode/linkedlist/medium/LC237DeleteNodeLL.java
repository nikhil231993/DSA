package leetcode.linkedlist.medium;

public class LC237DeleteNodeLL {

    /**
    public void deleteNode(ListNode node) {

        int val=node.next.val;
        node.next.val=node.val;
        node.val=val;

        node.next=node.next.next;

        TC:O(1)
        SC:O(1)
    }
     */
}
